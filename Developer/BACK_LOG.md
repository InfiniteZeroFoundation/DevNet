# Backlog — Unscheduled / Not Yet Phased

> Items identified but not yet assigned to a specific phase or formal Work Package.
> Triage into P3/P4/P5 (promote to a numbered WP row in [ROADMAP.md](ROADMAP.md)) as priorities firm up.
> Goal: keep a visible backlog of independent, scopeable tasks so assignment doesn't
> become reactive — open a corresponding GitHub issue as each item is triaged.

| ID | Domain | Item | Description | Candidate Phase | Source / Rationale | Status |
|----|--------|------|--------------|------------------|---------------------|--------|
| BL-1 | dincli / SDK | Nonce management for concurrent tx submission | `DinContext.get_tx_params()` (`cli/context.py`) fetches the nonce via `get_transaction_count(address)` with no block param — defaults to `"latest"` (confirmed only), so two transactions fired before the first confirms collide on the same nonce today. Needs a nonce manager/lock before `dind` submits jobs concurrently from one account. | P3 (bug fix) or P4-1.2 | Surfaced during SDK interface proposal review, issue #20 (Jul 23, 2026) | 🆕 New |
| BL-2 | Daemon / SDK | Wallet/session/tx signing keystone (`session.py`, `wallet.py`, `tx.py`) | `DinSession`, `SignerProvider`, and `tx.send()` — the private-key/signing surface of the SDK interface proposal (§2, §5b). Deliberately deferred out of task_220726_7 as its own focused review-and-build task given the security sensitivity. | P4-1.2 | task_220726_7 scope boundaries; SDK interface proposal v0.3 | 🆕 New |
| BL-3 | Daemon | Validator hardware load-bearing / load-balancing | P4-2.2 covers hardware *detection* and *scoring* for task eligibility, not balancing load across concurrent jobs a validator is running. Distinct, substantial follow-on once multi-task daemon automation exists. | P4/P5 | Staffing discussion, Jul 23, 2026 | 💭 Idea |
| BL-4 | Tooling | Client tooling layer | Dedicated tooling for clients (dataset configuration, etc.) — distinct from `dind`'s own automation (P4-4.1 covers daemon-driven client automation *once* configured, not the setup/config tooling itself). | P4/P5 | Staffing discussion, Jul 23, 2026 | 💭 Idea |
| BL-5 | Tooling | Model-owner tooling layer | Dedicated tooling to help model owners author `manifest.json`/service files (`model.py`, `modelowner.py`, `client.py`, `auditor.py`, `aggregator.py`) — explicitly beyond `dind`'s job. P4-5.1 covers IPFS/registration automation, not authoring assistance. | P4/P5 | SDK proposal §6 review (issue #20); staffing discussion, Jul 23, 2026 | 💭 Idea |
| BL-6 | Daemon | "Agentic dind" extension | Once P4 daemon automation is stable, extend `dind` toward more autonomous/agentic operation beyond scripted automation. Vision-stage, not yet scoped. | P5+ | Staffing discussion, Jul 23, 2026 | 💭 Idea |
| BL-7 | Protocol / Mechanism design | Evaluator geographic/infrastructure diversity monitoring | Distinct from the Sybil/identity gap already tracked (whitepaper-summary.md checklist item #13): honest, uniquely-staked Evaluators could still cluster behind the same cloud provider or jurisdiction (correlated-failure risk, akin to a Nakamoto Coefficient). Proposal is additive/measurement-only, not a slashing change — (1) capture operator metadata (region, hosting ASN, client version) at NFT-stake registration, (2) publish a concentration dashboard, (3) weight random Evaluator assignment toward diversity within the eligible pool. Depends on whatever DID/VC approach gets chosen for item #13, so blocked on that decision. | P3/P4 (depends on staking + identity design) | [Discussion #48](https://github.com/InfiniteZeroFoundation/DevNet/discussions/48), @robertocarlous, Jul 21, 2026 | 🆕 New |
| BL-8 | dincli | `import-deployments` still imports only four ProxyAdmin keys | `_DEPLOYMENTS_TO_DIN_INFO` ([dincli/cli/system.py](../dincli/cli/system.py)) maps four contracts + four `proxyAdmin*` keys; the foundry deployments JSON emits six of each since PR #51 (fee router). `dinTreasury`, `dinFeeRouter`, `proxyAdminTreasury`, `proxyAdminFeeRouter` are silently dropped by `import-deployments`. Nothing breaks today (dincli doesn't use those addresses yet). Also fix the docstring claim that "both flows write the same address schema" — hardhat still writes the four-proxy shape. | P3 | [PR #51](https://github.com/InfiniteZeroFoundation/DevNet/pull/51) review follow-up (merge d2b7383 extended the foundry deploy script to six proxies but not the dincli import side), Jul 24, 2026 | 🆕 New |
| BL-9 | Daemon / SDK | `sdk/operations/` layer + real `dind` job handlers | `to_envelope()` (`dincli/sdk/serialize.py`, PR #31) is built and tested but has zero production call sites — no SDK operation's dataclass return is ever routed through it, and `dind`'s `JOB_HANDLERS` registry (PR #32) has exactly one job type (`"demo"`, a no-op). The `sdk/operations/` layer that would call real SDK functions (task registration, GI lifecycle, aggregator/auditor scoring, etc.) and feed their dataclass returns through `to_envelope()` for both `dind`'s job queue and the CLI's future `--json` output doesn't exist yet in either PR. Expected sequencing, not a regression — `operations/` is gated behind the wallet/session/tx keystone (BL-2); real job types (client/auditor/aggregator role automation) get wired in once that lands. | P4-1.2 | PR #31/#32 code review follow-up, Jul 30, 2026 | 🆕 New |
| BL-10 | Solidity/foundry (rewards) | Reward settlement doubles the C-1 unbounded-loop gas-DoS surface | `DINTaskAuditor.settleRewards` (called from `DINTaskCoordinator.endGI`, task_210726_6 §3) iterates every client/auditor/aggregator to compute and credit shares, stacking on top of the existing per-GI iteration in `slashAuditors`/`slashAggregators`. Extends the already-tracked C-1 finding (2026-07 security review, `Developer/audits/2026-07_foundry-src-security-review.md`) to roughly 2x its worst measured cost at spec scale (10–50 validators / 100–500 clients). Needs a paginated or claim-time-computed settlement redesign before spec-scale deployment — same treatment C-1 itself is waiting on. | P3/P4 | PR #64 review, Jul 30, 2026 | 🆕 New |
