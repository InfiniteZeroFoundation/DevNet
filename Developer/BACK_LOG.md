# Backlog — Unscheduled / Not Yet Phased

> Items identified but not yet assigned to a specific phase or formal Work Package.
> Triage into P3/P4/P5 (promote to a numbered WP row in [ROADMAP.md](ROADMAP.md)) as priorities firm up.
> Goal: keep a visible backlog of independent, scopeable tasks so assignment doesn't
> become reactive — open a corresponding GitHub issue as each item is triaged.

| ID | Domain | Item | Description | Candidate Phase | Source / Rationale | Status |
|----|--------|------|--------------|------------------|---------------------|--------|
| BL-1 | dincli / SDK | Nonce management for concurrent tx submission | `DinContext.get_tx_params()` (`cli/context.py`) fetches the nonce via `get_transaction_count(address)` with no block param — defaults to `"latest"` (confirmed only), so two transactions fired before the first confirms collide on the same nonce today. Needs a nonce manager/lock before `dind` submits jobs concurrently from one account. | P3 (bug fix) or P4-1.2 | Surfaced during SDK interface proposal review, issue #20 (Jul 23, 2026) | 🆕 New |
| BL-2 | Daemon / SDK | Wallet/session/tx signing keystone (`session.py`, `wallet.py`, `tx.py`) | `DinSession`, `SignerProvider`, and `tx.send()` — the private-key/signing surface of the SDK interface proposal (§2, §5b). Deliberately deferred out of task_220726_7 as its own focused review-and-build task given the security sensitivity. | P4-1.2 | task_220726_7 scope boundaries; SDK interface proposal v0.3 | 🆕 New |
| BL-3 | Daemon | Validator hardware load-bearing / load-balancing | P4-2.2 covers hardware *detection* and *scoring* for task eligibility, not balancing load across concurrent jobs a validator is running. Distinct, substantial follow-on once multi-task daemon automation exists. | P4/P5 | Staffing discussion, Jul 23, 2026 | 💭 Idea |
| BL-4 | Tooling | Client tooling layer | Dedicated tooling for clients (dataset configuration, etc.) — distinct from `dind`'s own automation (P4-4.1 covers daemon-driven client automation *once* configured, not the setup/config tooling itself). | P4/P5 | Staffing discussion, Jul 23, 2026 | 💭 Idea |
| BL-5 | Tooling | Model-owner tooling layer | Dedicated tooling to help model owners author `manifest.json`/service files (`model.py`, `modelowner.py`, `client.py`, `auditor.py`, `aggregator.py`) — explicitly beyond `dind`'s job. P4-5.1 covers IPFS/registration automation, not authoring assistance. | P4/P5 | SDK proposal §6 review (issue #20); staffing discussion, Jul 23, 2026 | 💭 Idea |
| BL-6 | Daemon | "Agentic dind" extension | Once P4 daemon automation is stable, extend `dind` toward more autonomous/agentic operation beyond scripted automation. Vision-stage, not yet scoped. | P5+ | Staffing discussion, Jul 23, 2026 | 💭 Idea |
| BL-7 | Protocol / Mechanism design | Evaluator geographic/infrastructure diversity monitoring | Distinct from the Sybil/identity gap already tracked (whitepaper-summary.md checklist item #13): honest, uniquely-staked Evaluators could still cluster behind the same cloud provider or jurisdiction (correlated-failure risk, akin to a Nakamoto Coefficient). Proposal is additive/measurement-only, not a slashing change — (1) capture operator metadata (region, hosting ASN, client version) at NFT-stake registration, (2) publish a concentration dashboard, (3) weight random Evaluator assignment toward diversity within the eligible pool. Depends on whatever DID/VC approach gets chosen for item #13, so blocked on that decision. | P3/P4 (depends on staking + identity design) | [Discussion #48](https://github.com/InfiniteZeroFoundation/DevNet/discussions/48), @robertocarlous, Jul 21, 2026 | 🆕 New |
| BL-8 | dincli | `import-deployments` still imports only four ProxyAdmin keys | `_DEPLOYMENTS_TO_DIN_INFO` ([dincli/cli/system.py](../dincli/cli/system.py)) maps four contracts + four `proxyAdmin*` keys; the foundry deployments JSON emits six of each since PR #51 (fee router). `dinTreasury`, `dinFeeRouter`, `proxyAdminTreasury`, `proxyAdminFeeRouter` are silently dropped by `import-deployments`. Nothing breaks today (dincli doesn't use those addresses yet). Also fix the docstring claim that "both flows write the same address schema" — hardhat still writes the four-proxy shape. | P3 | [PR #51](https://github.com/InfiniteZeroFoundation/DevNet/pull/51) review follow-up (merge d2b7383 extended the foundry deploy script to six proxies but not the dincli import side), Jul 24, 2026 | 🆕 New |
| BL-9 | Daemon / SDK | `sdk/operations/` layer + real `dind` job handlers | `to_envelope()` (`dincli/sdk/serialize.py`, PR #31) is built and tested but has zero production call sites — no SDK operation's dataclass return is ever routed through it, and `dind`'s `JOB_HANDLERS` registry (PR #32) has exactly one job type (`"demo"`, a no-op). The `sdk/operations/` layer that would call real SDK functions (task registration, GI lifecycle, aggregator/auditor scoring, etc.) and feed their dataclass returns through `to_envelope()` for both `dind`'s job queue and the CLI's future `--json` output doesn't exist yet in either PR. Expected sequencing, not a regression — `operations/` is gated behind the wallet/session/tx keystone (BL-2); real job types (client/auditor/aggregator role automation) get wired in once that lands. | P4-1.2 | PR #31/#32 code review follow-up, Jul 30, 2026 | 🆕 New |
| BL-10 | Solidity/foundry (rewards) | Reward settlement doubles the C-1 unbounded-loop gas-DoS surface | `DINTaskAuditor.settleRewards` (called from `DINTaskCoordinator.endGI`, task_210726_6 §3) iterates every client/auditor/aggregator to compute and credit shares, stacking on top of the existing per-GI iteration in `slashAuditors`/`slashAggregators`. Extends the already-tracked C-1 finding (2026-07 security review, `Developer/audits/2026-07_foundry-src-security-review.md`) to roughly 2x its worst measured cost at spec scale (10–50 validators / 100–500 clients). Needs a paginated or claim-time-computed settlement redesign before spec-scale deployment — same treatment C-1 itself is waiting on. | P3/P4 | PR #64 review, Jul 30, 2026 | 🆕 New |
| BL-11 | Solidity/foundry (slashing, dispute resolution) | Dispute-redo aggregator selection reuses the grindable blockhash shuffle (H-2) | `DINTaskCoordinator._assignFreshSubgroup` (task_210726_6 §4c dispute scaffold) shuffles via the same `blockhash(block.number - 1)`-based entropy flagged as finding H-2 in the 2026-07 security review. Materially worse here: the grinding party (model owner, via `resolveDispute`) is also the sole adjudicator and faces no deadline, so they can wait indefinitely for a favorable hash — undermining the one mechanism meant to catch owner/aggregator collusion. Should be resolved together with H-2 (commit-reveal or VRF-based randomness) before dispute resolution graduates past scaffold. | P3/P4 | PR #66 review, linked to H-2 (2026-07 security review), Jul 30, 2026 | 🆕 New |
| BL-12 | Protocol / Mechanism design | Dispute bond denomination (DIN) vs. MECHANISM_DESIGN's ETH-denominated validator-fee bonds | `DINTaskCoordinator`'s dispute bond (task_210726_6 §4c) is DIN-denominated, while `MECHANISM_DESIGN.md`'s resolved decision (2026-07-21) places validator-network-fee bonds in ETH. Not necessarily wrong — dispute bonds and network-fee bonds may reasonably use different denominations — but wasn't flagged as a deliberate choice anywhere, including the slashing taxonomy doc. Needs an explicit decision recorded either way. | P3 | PR #66 review, Jul 30, 2026 | 🆕 New |
| BL-13 | dincli / Auditing | Encrypted test-data key correspondence enforced only by array length, not identity | `DINTaskAuditor.assignAuditTestDataset` checks `encryptedKeys.length == batch.auditors.length` but has no way to verify `encryptedKeys[i]` actually corresponds to `auditors[i]` — a misordered off-chain script could silently hand an auditor the wrong encrypted key. Fix likely belongs on the dincli side (keying the off-chain encryption step explicitly by auditor address rather than positional order) once real per-validator encryption is implemented — PR #63 only wired the on-chain plumbing, per task scope. | P3/P4 | PR #63 review, Jul 30, 2026 | 🆕 New |
| BL-14 | Solidity/foundry (rewards) | Per-recipient bps-split rounding dust never swept | `DINTaskAuditor.settleRewards`'s integer-division bps split leaves small per-recipient rounding remainders uncollected — no fund-safety impact (funds stay in the contract, just uncredited to any specific claimant), but nothing sweeps it to treasury either. Minor cleanup once the treasury-forwarding TODO from Part 3 (task_210726_6 §3) is resolved. | P3/P4 | PR #64 review, Jul 30, 2026 | 🆕 New |
| BL-15 | Solidity/foundry (slashing, dispute resolution) | A resolved dispute can never be reopened against the same batch — undocumented | `DINTaskCoordinator.resolveDispute` has no path back to `disputes[gi][tierKind][batchId].resolved == false` once set, so exactly one dispute per batch is possible, ever (upheld or rejected). Reasonable as a scaffold-stage design choice, but wasn't called out in code comments or `Developer/design/slashing-taxonomy.md`. Cheap doc fix; no behavior change needed unless multi-dispute-per-batch is later desired. | P3/P4 | PR #66 review, Jul 30, 2026 | 🆕 New |
| BL-16 | Governance / din-dao | DIN-DAO (Stages A–D) deferred to post-mainnet | Abraham's decision: no `DinMultisig` for Stage A/Devnet 2.0–3.0 — DevNet governs off-chain (team coordination + public discussion, Ethereum All-Core-Devs-style) until well past testing; any near-term multisig is a plain Gnosis Safe scoped to treasury/funds only, never protocol roles. This pulls all of `feat/din-dao` (PR #27, issue #23) out of active P4 scope, not just the Stage A multisig — Stage C (`DinGovernor`/`DinGovernanceStaking`) goes with it. [DD-1](design/DESIGN_DECISIONS.md#dd-1--validator-governance-voting-power-double-locking-vs-stake-bridge), [DD-2](design/DESIGN_DECISIONS.md#dd-2--dao-voting-power-model-coin-weighted-vs-quadratic-vs-non-coin-based), and [DD-3](design/DESIGN_DECISIONS.md#dd-3--initial-dinmultisig-signer-composition-stage-a) are the specific design questions that move with it — DD-3 is resolved (no Stage A multisig at all), DD-1/DD-2 (Stage C voting power/model) stay open and unresolved, to revisit once on-chain governance work actually resumes. In the interim, governance discussion moves to forums. | Post-mainnet | discuss-040826.md (Abraham, Aug 4, 2026); [discussion #70](https://github.com/InfiniteZeroFoundation/DevNet/discussions/70) comment (robertocarlous, Aug 4, 2026) confirming the team-call decision | 🆕 New |
| BL-17 | Cryptoeconomics / Tokenomics | Curated per-model staking & fee attribution | Robert's follow-up to the a16z tokenomics piece Abraham shared: today validator staking is one shared network-wide pool and `DinFeeRouter` distributes revenue without attribution to individual models, so incentives track network-wide averages rather than any one model's performance. Proposal: let validators stake toward individual models/model owners via `DINModelRegistry` instead, using the `modelMinStakeBounds` storage already added to `DinValidatorStake` during P3 staking work, making fee flows traceable per model (closer to an Aave Safety-Module framing than a passive-holder dividend). Most of the wiring already exists; remaining work is mainly task-contract-level enforcement, already flagged as a `modelMinStakeBounds` follow-up. Robert explicitly leaves P4-vs-P5 placement open. Article summary + context: [`discussion/application-token-cash-flows-a16z.md`](discussion/application-token-cash-flows-a16z.md). | P4/P5 (undecided) | [Discussion #73](https://github.com/InfiniteZeroFoundation/DevNet/discussions/73), @robertocarlous, Aug 5, 2026 | 🆕 New |
| BL-18 | Cryptoeconomics / Tokenomics | Fair-launch validator token distribution | Abraham's vision (Slack, Jul 31, 2026): no ICO, pre-sale, or VC allocation — DIN distributed exclusively to validators/active network participants, programmatically, as a fair launch. Technical blueprint sent Aug 5, 2026: a new Merkle-drop claim contract (Transparent Proxy, treasury-Safe admin per [DD-3](design/DESIGN_DECISIONS.md#dd-3--initial-dinmultisig-signer-composition-stage-a)) funded once from treasury, cliff + linear vesting on claim. Resolves the open ICO-vs-airdrop fork of [`MECHANISM_DESIGN.md` §9 item 14](design/MECHANISM_DESIGN.md#9-consolidated-open-decision-list) in favor of the validator-airdrop path already named in §9-8. Abraham confirmed Aug 5, 2026 (Slack, 5:12 PM): testnet dry-run, move fast — local anvil → devnet → testnet → mainnet path. Phase set to P4 (Umer, Aug 5, 2026): eligibility computed off the on-chain indexer (P4-IDX1/P4-IDX2, not a raw RPC-log script) rather than directly off `DinValidatorStake`/`DINTaskAuditor`/`DINTaskCoordinator` events, run by the treasury-admin authority once the indexer exists. Contract-only slice assigned to Robbert: [`task_050826_9`](tasks/task_050826_9.md) (`feat/fair-launch-distributor`, off `develop` HEAD, no other-PR contract dependency identified) — tracked via [issue #75](https://github.com/InfiniteZeroFoundation/DevNet/issues/75) and [discussion #76](https://github.com/InfiniteZeroFoundation/DevNet/discussions/76). Eligibility-computation slice deferred pending P4-IDX1/IDX2. | P4 | Abraham Slack message, Jul 31, 2026 (`HR/Abraham/questions-310726.md`); reply drafted, Aug 5, 2026 (`HR/Umer/reply-abraham-050826-310726.md`); Abraham's phase/network answer + Umer's P4/indexer scoping, Aug 5, 2026 (`HR/Abraham/discuss-040826.md`) | ⚡ In Progress |
